kind: pipeline
name: pipeline-example-maven

steps:
  - name: build-maven
    image: maven:3-jdk-7
    volumes:
      - name: cache
        path: /root/.m2
    commands:
      - pwd
      - ls -l
      - mvn clean install